///
/// HybridPoseLandmarkerOutputSpec_cxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * A class implementation that bridges HybridPoseLandmarkerOutputSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
open class HybridPoseLandmarkerOutputSpec_cxx : HybridCameraOutputSpec_cxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::aiposesuggestor::poseplugin::bridge::swift`)
   * from `AIPoseSuggestorPosePlugin-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.aiposesuggestor.poseplugin.bridge.swift

  /**
   * Holds an instance of the `HybridPoseLandmarkerOutputSpec` Swift protocol.
   */
  private var __implementation: any HybridPoseLandmarkerOutputSpec

  /**
   * Holds a weak pointer to the C++ class that wraps the Swift class.
   */
  private var __cxxPart: bridge.std__weak_ptr_HybridPoseLandmarkerOutputSpec_

  /**
   * Create a new `HybridPoseLandmarkerOutputSpec_cxx` that wraps the given `HybridPoseLandmarkerOutputSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: any HybridPoseLandmarkerOutputSpec) {
    self.__implementation = implementation
    self.__cxxPart = .init()
    super.init(implementation)
  }

  /**
   * Get the actual `HybridPoseLandmarkerOutputSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridPoseLandmarkerOutputSpec() -> any HybridPoseLandmarkerOutputSpec {
    return __implementation
  }

  /**
   * Casts this instance to a retained unsafe raw pointer.
   * This acquires one additional strong reference on the object!
   */
  public override func toUnsafe() -> UnsafeMutableRawPointer {
    return Unmanaged.passRetained(self).toOpaque()
  }

  /**
   * Casts an unsafe pointer to a `HybridPoseLandmarkerOutputSpec_cxx`.
   * The pointer has to be a retained opaque `Unmanaged<HybridPoseLandmarkerOutputSpec_cxx>`.
   * This removes one strong reference from the object!
   */
  public override class func fromUnsafe(_ pointer: UnsafeMutableRawPointer) -> HybridPoseLandmarkerOutputSpec_cxx {
    return Unmanaged<HybridPoseLandmarkerOutputSpec_cxx>.fromOpaque(pointer).takeRetainedValue()
  }

  /**
   * Gets (or creates) the C++ part of this Hybrid Object.
   * The C++ part is a `std::shared_ptr<HybridPoseLandmarkerOutputSpec>`.
   */
  public func getCxxPart() -> bridge.std__shared_ptr_HybridPoseLandmarkerOutputSpec_ {
    let cachedCxxPart = self.__cxxPart.lock()
    if Bool(fromCxx: cachedCxxPart) {
      return cachedCxxPart
    } else {
      let newCxxPart = bridge.create_std__shared_ptr_HybridPoseLandmarkerOutputSpec_(self.toUnsafe())
      __cxxPart = bridge.weakify_std__shared_ptr_HybridPoseLandmarkerOutputSpec_(newCxxPart)
      return newCxxPart
    }
  }

  public override func getCxxPart() -> bridge.std__shared_ptr_margelo__nitro__camera__HybridCameraOutputSpec_ {
    let ownCxxPart: bridge.std__shared_ptr_HybridPoseLandmarkerOutputSpec_ = getCxxPart()
    return bridge.upcast_PoseLandmarkerOutput_to_CameraOutput(ownCxxPart)
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public override var memorySize: Int {
    return MemoryHelper.getSizeOf(self.__implementation) + self.__implementation.memorySize
  }

  /**
   * Compares this object with the given [other] object for reference equality.
   */
  @inline(__always)
  public func equals(other: HybridPoseLandmarkerOutputSpec_cxx) -> Bool {
    return self.__implementation === other.__implementation
  }

  /**
   * Call dispose() on the Swift class.
   * This _may_ be called manually from JS.
   */
  @inline(__always)
  public override func dispose() {
    self.__implementation.dispose()
  }

  /**
   * Call toString() on the Swift class.
   */
  @inline(__always)
  public override func toString() -> String {
    return self.__implementation.toString()
  }

  // Properties
  

  // Methods
  @inline(__always)
  public final func setOnResultsCallback(onResults: bridge.std__optional_std__function_void_const_PoseLandmarkResult_____result______) -> bridge.Result_void_ {
    do {
      try self.__implementation.setOnResultsCallback(onResults: { () -> ((_ result: PoseLandmarkResult) -> Void)? in
        if bridge.has_value_std__optional_std__function_void_const_PoseLandmarkResult_____result______(onResults) {
          let __unwrapped = bridge.get_std__optional_std__function_void_const_PoseLandmarkResult_____result______(onResults)
          return { () -> (PoseLandmarkResult) -> Void in
            let __wrappedFunction = bridge.wrap_Func_void_PoseLandmarkResult(__unwrapped)
            return { (__result: PoseLandmarkResult) -> Void in
              __wrappedFunction.call(__result)
            }
          }()
        } else {
          return nil
        }
      }())
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
}
